//use std::{thread, time};

//use std::io::Error;

// extern crate rand;

// use std::io;
// use rand::Rng;

mod window;

fn main()
{
    /* println!("Hello, world!");

    let ten_secs = time::Duration::from_secs(10);

    thread::sleep(ten_secs); */

    window::main()
}

